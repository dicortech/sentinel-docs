"use strict";(self.webpackChunkmy_website=self.webpackChunkmy_website||[]).push([[5140],{13368:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>s,contentTitle:()=>o,default:()=>m,frontMatter:()=>c,metadata:()=>a,toc:()=>l});const a=JSON.parse('{"id":"Software-Documentation/sdk/library/pid_otrack/usage-examples","title":"Usage Examples","description":"Practical examples for using the PID Object Tracker library","source":"@site/docs/Software-Documentation/sdk/library/pid_otrack/usage-examples.mdx","sourceDirName":"Software-Documentation/sdk/library/pid_otrack","slug":"/Software-Documentation/sdk/library/pid_otrack/usage-examples","permalink":"/sentinel-docs/docs/canary/Software-Documentation/sdk/library/pid_otrack/usage-examples","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":4,"frontMatter":{"title":"Usage Examples","description":"Practical examples for using the PID Object Tracker library","sidebar_position":4},"sidebar":"tutorialSidebar","previous":{"title":"API Reference","permalink":"/sentinel-docs/docs/canary/Software-Documentation/sdk/library/pid_otrack/api-reference"}}');var i=t(74848),r=t(28453);const c={title:"Usage Examples",description:"Practical examples for using the PID Object Tracker library",sidebar_position:4},o=void 0,s={},l=[{value:"Example 1: Basic Tracking (Manual Updates)",id:"example-1-basic-tracking-manual-updates",level:2},{value:"Example 2: MQTT-Based Tracking",id:"example-2-mqtt-based-tracking",level:2},{value:"Example 3: ROI-Based Tracking",id:"example-3-roi-based-tracking",level:2},{value:"Example 4: PID Tuning for Different Scenarios",id:"example-4-pid-tuning-for-different-scenarios",level:2},{value:"Example 5: Dynamic Topic Switching",id:"example-5-dynamic-topic-switching",level:2},{value:"Example 6: Interactive Target Selection",id:"example-6-interactive-target-selection",level:2},{value:"Example 7: Multi-Resolution Support",id:"example-7-multi-resolution-support",level:2},{value:"Common Patterns",id:"common-patterns",level:2},{value:"Error Handling",id:"error-handling",level:3},{value:"Debug Logging",id:"debug-logging",level:3},{value:"Status Monitoring",id:"status-monitoring",level:3},{value:"Integration with GStreamer",id:"integration-with-gstreamer",level:2}];function d(e){const n={code:"code",h2:"h2",h3:"h3",p:"p",pre:"pre",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.p,{children:"This page provides practical examples demonstrating various use cases of the PID Object Tracker library."}),"\n",(0,i.jsx)(n.h2,{id:"example-1-basic-tracking-manual-updates",children:"Example 1: Basic Tracking (Manual Updates)"}),"\n",(0,i.jsx)(n.p,{children:"This example shows basic object tracking with manual detection updates, suitable for integration with custom detection pipelines."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:'#include "lib-pid-otrack.h"\n\nusing namespace DctotrackPID;\n\nint main() {\n    // Initialize tracker with model and display resolutions\n    FrameResolution model_res(640, 384);\n    FrameResolution display_res(1920, 1080);\n    ObjectTrackerLib tracker(model_res, display_res);\n\n    // Configure tracker\n    tracker.initializeWithConfig("my_app");\n    tracker.ai_obj_trk_set_target_id(1);\n    tracker.ai_obj_trk_set_mode(TrackingMode::CENTROID);\n    tracker.ai_obj_trk_enable();\n\n    // Main tracking loop\n    while (running) {\n        // Get detections from your detection pipeline\n        std::vector<DetectedObject> detections = getDetectionsFromPipeline();\n\n        // Update tracker with new detections\n        tracker.ai_obj_trk_update_detections(detections);\n\n        // Calculate pan/tilt command\n        PanTiltCommand cmd = tracker.calculatePanTiltCommand();\n\n        if (cmd.valid) {\n            printf("Pan: %.2f, Tilt: %.2f\\n", cmd.pan_delta, cmd.tilt_delta);\n            // Send commands to PTZ controller\n            sendToPTZController(cmd.pan_delta, cmd.tilt_delta);\n        }\n\n        usleep(33000);  // ~30 Hz control rate\n    }\n\n    return 0;\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"example-2-mqtt-based-tracking",children:"Example 2: MQTT-Based Tracking"}),"\n",(0,i.jsx)(n.p,{children:"This example demonstrates automatic tracking with MQTT-based detection updates, ideal for distributed systems."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:'#include "mqtt-object-tracker.h"\n\nusing namespace DctotrackPID;\n\nint main() {\n    // Initialize MQTT tracker\n    FrameResolution model_res(640, 384);\n    FrameResolution display_res(1920, 1080);\n    MQTTObjectTracker mqtt_tracker(model_res, display_res);\n\n    // Configure MQTT connection\n    MQTTConfig mqtt_config;\n    mqtt_config.broker_host = "192.168.1.100";\n    mqtt_config.broker_port = 1883;\n    mqtt_config.client_id = "camera_tracker";\n    mqtt_config.detection_topic = "ai/detections/camera1";\n    mqtt_config.username = "";  // Optional\n    mqtt_config.password = "";  // Optional\n\n    if (!mqtt_tracker.initializeMQTT(mqtt_config)) {\n        fprintf(stderr, "Failed to initialize MQTT\\n");\n        return -1;\n    }\n\n    if (!mqtt_tracker.startMQTT()) {\n        fprintf(stderr, "Failed to start MQTT\\n");\n        return -1;\n    }\n\n    // Configure tracking parameters\n    auto* tracker = mqtt_tracker.getTracker();\n    tracker->initializeWithConfig("mqtt_app");\n    tracker->ai_obj_trk_set_target_id(1);\n    tracker->ai_obj_trk_enable();\n\n    // Main tracking loop\n    // Detections are automatically received via MQTT\n    while (running) {\n        if (tracker->ai_obj_trk_is_enabled()) {\n            PanTiltCommand cmd = tracker->calculatePanTiltCommand();\n\n            if (cmd.valid) {\n                // Control camera based on cmd.pan_delta and cmd.tilt_delta\n                control_camera(cmd.pan_delta, cmd.tilt_delta);\n            }\n        }\n\n        usleep(100000);  // 10 Hz control rate\n    }\n\n    // Cleanup\n    mqtt_tracker.stopMQTT();\n    return 0;\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"example-3-roi-based-tracking",children:"Example 3: ROI-Based Tracking"}),"\n",(0,i.jsx)(n.p,{children:"This example shows how to use Region of Interest (ROI) tracking mode, where the camera only moves when the object approaches ROI boundaries."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:'#include "lib-pid-otrack.h"\n\nusing namespace DctotrackPID;\n\nint main() {\n    // Initialize tracker\n    ObjectTrackerLib tracker(\n        FrameResolution(640, 384),\n        FrameResolution(1920, 1080)\n    );\n\n    tracker.initializeWithConfig("roi_app");\n\n    // Configure ROI mode with custom ROI\n    tracker.ai_obj_trk_set_mode(TrackingMode::ROI);\n\n    // Define ROI as center 60% of frame\n    float roi_x = 1920 * 0.2;       // 20% from left\n    float roi_y = 1080 * 0.2;       // 20% from top\n    float roi_width = 1920 * 0.6;   // 60% of width\n    float roi_height = 1080 * 0.6;  // 60% of height\n\n    tracker.ai_obj_trk_set_roi(roi_x, roi_y, roi_width, roi_height);\n\n    // Select target and enable tracking\n    tracker.ai_obj_trk_set_target_id(1);\n    tracker.ai_obj_trk_enable();\n\n    // Main tracking loop\n    while (running) {\n        std::vector<DetectedObject> detections = getDetections();\n        tracker.ai_obj_trk_update_detections(detections);\n\n        PanTiltCommand cmd = tracker.calculatePanTiltCommand();\n\n        if (cmd.valid) {\n            // Camera only moves when object approaches ROI edges\n            controlPTZ(cmd.pan_delta, cmd.tilt_delta);\n        }\n\n        usleep(33000);\n    }\n\n    return 0;\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"example-4-pid-tuning-for-different-scenarios",children:"Example 4: PID Tuning for Different Scenarios"}),"\n",(0,i.jsx)(n.p,{children:"This example demonstrates how to tune PID parameters for different tracking scenarios."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:'#include "lib-pid-otrack.h"\n\nusing namespace DctotrackPID;\n\n// Aggressive tracking (fast response, may be jittery)\nvoid configureAggressiveTracking(ObjectTrackerLib& tracker) {\n    tracker.ai_obj_trk_set_pid_gains(\n        0.20f,  // pan_kp - higher for faster response\n        0.002f, // pan_ki\n        0.020f, // pan_kd\n        0.20f,  // tilt_kp\n        0.002f, // tilt_ki\n        0.020f  // tilt_kd\n    );\n\n    tracker.ai_obj_trk_set_pid_limits(-80.0f, 80.0f);\n    tracker.ai_obj_trk_set_filtering_params(0.02f, 0.6f);  // Less smoothing\n}\n\n// Smooth tracking (slower response, more stable)\nvoid configureSmoothTracking(ObjectTrackerLib& tracker) {\n    tracker.ai_obj_trk_set_pid_gains(\n        0.08f,  // pan_kp - lower for gentler response\n        0.001f, // pan_ki\n        0.012f, // pan_kd - higher to reduce overshoot\n        0.08f,  // tilt_kp\n        0.001f, // tilt_ki\n        0.012f  // tilt_kd\n    );\n\n    tracker.ai_obj_trk_set_pid_limits(-50.0f, 50.0f);\n    tracker.ai_obj_trk_set_filtering_params(0.05f, 0.9f);  // More smoothing\n}\n\n// Balanced tracking (default settings)\nvoid configureBalancedTracking(ObjectTrackerLib& tracker) {\n    tracker.ai_obj_trk_set_pid_gains(\n        0.10f,  // pan_kp\n        0.001f, // pan_ki\n        0.015f, // pan_kd\n        0.10f,  // tilt_kp\n        0.001f, // tilt_ki\n        0.015f  // tilt_kd\n    );\n\n    tracker.ai_obj_trk_set_pid_limits(-60.0f, 60.0f);\n    tracker.ai_obj_trk_set_filtering_params(0.03f, 0.8f);\n}\n\nint main() {\n    ObjectTrackerLib tracker(\n        FrameResolution(640, 384),\n        FrameResolution(1920, 1080)\n    );\n\n    tracker.initializeWithConfig("tuning_app");\n\n    // Choose tracking style based on your needs\n    configureSmoothTracking(tracker);  // For surveillance cameras\n    // configureAggressiveTracking(tracker);  // For sports tracking\n    // configureBalancedTracking(tracker);    // For general use\n\n    tracker.ai_obj_trk_set_target_id(1);\n    tracker.ai_obj_trk_enable();\n\n    // Tracking loop...\n\n    return 0;\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"example-5-dynamic-topic-switching",children:"Example 5: Dynamic Topic Switching"}),"\n",(0,i.jsx)(n.p,{children:"This example shows how to dynamically switch between different MQTT topics at runtime, useful for multi-camera systems."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:'#include "mqtt-object-tracker.h"\n\nusing namespace DctotrackPID;\n\nvoid switchToCamera(MQTTObjectTracker& tracker,\n                    const std::string& old_topic,\n                    const std::string& new_topic) {\n    // Unsubscribe from old camera topic\n    if (!old_topic.empty()) {\n        tracker.unsubscribe(old_topic);\n        printf("Unsubscribed from: %s\\n", old_topic.c_str());\n    }\n\n    // Subscribe to new camera topic\n    if (tracker.subscribe(new_topic, 1)) {\n        tracker.setDetectionTopic(new_topic);\n        printf("Switched to: %s\\n", new_topic.c_str());\n    } else {\n        fprintf(stderr, "Failed to subscribe to: %s\\n", new_topic.c_str());\n    }\n}\n\nint main() {\n    MQTTObjectTracker tracker(\n        FrameResolution(640, 384),\n        FrameResolution(1920, 1080)\n    );\n\n    // Initialize MQTT\n    MQTTConfig config;\n    config.broker_host = "localhost";\n    config.broker_port = 1883;\n    config.client_id = "multi_camera_tracker";\n    config.detection_topic = "camera/cam1/detections";\n\n    tracker.initializeMQTT(config);\n    tracker.startMQTT();\n\n    auto* obj_tracker = tracker.getTracker();\n    obj_tracker->initializeWithConfig("multi_cam_app");\n    obj_tracker->ai_obj_trk_enable();\n\n    std::string current_topic = "camera/cam1/detections";\n\n    // Main loop\n    while (running) {\n        // Check for camera switch command (e.g., from user input)\n        if (shouldSwitchCamera()) {\n            std::string new_topic = getNewCameraTopic();\n            switchToCamera(tracker, current_topic, new_topic);\n            current_topic = new_topic;\n        }\n\n        // Process tracking\n        PanTiltCommand cmd = obj_tracker->calculatePanTiltCommand();\n        if (cmd.valid) {\n            controlCamera(cmd.pan_delta, cmd.tilt_delta);\n        }\n\n        usleep(100000);\n    }\n\n    tracker.stopMQTT();\n    return 0;\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"example-6-interactive-target-selection",children:"Example 6: Interactive Target Selection"}),"\n",(0,i.jsx)(n.p,{children:"This example demonstrates interactive target selection by clicking on display coordinates."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:'#include "lib-pid-otrack.h"\n#include <iostream>\n\nusing namespace DctotrackPID;\n\n// Simulate user clicking at display coordinates\nvoid handleUserClick(ObjectTrackerLib& tracker, float click_x, float click_y) {\n    printf("User clicked at (%.1f, %.1f)\\n", click_x, click_y);\n\n    if (tracker.ai_obj_trk_set_by_coordinate(click_x, click_y)) {\n        int selected_id = tracker.ai_obj_trk_get_target_id();\n        printf("Selected object ID: %d\\n", selected_id);\n\n        DetectedObject target;\n        if (tracker.ai_obj_trk_get_current_target(target)) {\n            printf("Target at (%.1f, %.1f) with confidence %.2f\\n",\n                   target.x, target.y, target.confidence);\n        }\n    } else {\n        printf("No object found near click position\\n");\n    }\n}\n\n// Display available objects\nvoid showAvailableObjects(ObjectTrackerLib& tracker) {\n    auto ids = tracker.ai_obj_trk_get_available_ids();\n\n    printf("Available object IDs: ");\n    for (int id : ids) {\n        printf("%d ", id);\n    }\n    printf("\\n");\n}\n\nint main() {\n    ObjectTrackerLib tracker(\n        FrameResolution(640, 384),\n        FrameResolution(1920, 1080)\n    );\n\n    tracker.initializeWithConfig("interactive_app");\n    tracker.ai_obj_trk_set_mode(TrackingMode::CENTROID);\n    tracker.ai_obj_trk_enable();\n\n    while (running) {\n        // Get detections\n        std::vector<DetectedObject> detections = getDetections();\n        tracker.ai_obj_trk_update_detections(detections);\n\n        // Show available objects\n        showAvailableObjects(tracker);\n\n        // Simulate user interaction\n        // In a real app, this would come from mouse clicks or touch events\n        if (userClickedScreen()) {\n            float click_x, click_y;\n            getUserClickCoordinates(click_x, click_y);\n            handleUserClick(tracker, click_x, click_y);\n        }\n\n        // Calculate and apply tracking commands\n        PanTiltCommand cmd = tracker.calculatePanTiltCommand();\n        if (cmd.valid) {\n            controlPTZ(cmd.pan_delta, cmd.tilt_delta);\n        }\n\n        usleep(33000);\n    }\n\n    return 0;\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"example-7-multi-resolution-support",children:"Example 7: Multi-Resolution Support"}),"\n",(0,i.jsx)(n.p,{children:"This example shows how to dynamically update resolutions when switching between different video sources."}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:'#include "lib-pid-otrack.h"\n\nusing namespace DctotrackPID;\n\nstruct CameraConfig {\n    int model_width, model_height;\n    int display_width, display_height;\n    std::string name;\n};\n\nvoid switchCamera(ObjectTrackerLib& tracker, const CameraConfig& config) {\n    printf("Switching to camera: %s\\n", config.name.c_str());\n    printf("Model res: %dx%d, Display res: %dx%d\\n",\n           config.model_width, config.model_height,\n           config.display_width, config.display_height);\n\n    // Update resolutions\n    tracker.ai_obj_trk_set_resolutions(\n        config.model_width, config.model_height,\n        config.display_width, config.display_height\n    );\n\n    // Reset PID controllers when switching cameras\n    tracker.ai_obj_trk_reset_pid_controllers();\n}\n\nint main() {\n    // Define camera configurations\n    CameraConfig cam1 = {640, 384, 1920, 1080, "HD Camera 1"};\n    CameraConfig cam2 = {320, 192, 1280, 720, "SD Camera 2"};\n    CameraConfig cam3 = {640, 384, 3840, 2160, "4K Camera 3"};\n\n    // Initialize with first camera\n    ObjectTrackerLib tracker(\n        FrameResolution(cam1.model_width, cam1.model_height),\n        FrameResolution(cam1.display_width, cam1.display_height)\n    );\n\n    tracker.initializeWithConfig("multi_res_app");\n    tracker.ai_obj_trk_enable();\n\n    CameraConfig* current_cam = &cam1;\n\n    while (running) {\n        // Check for camera switch\n        if (shouldSwitchCamera()) {\n            CameraConfig* new_cam = selectNewCamera();\n            switchCamera(tracker, *new_cam);\n            current_cam = new_cam;\n        }\n\n        // Normal tracking operation\n        std::vector<DetectedObject> detections = getDetections();\n        tracker.ai_obj_trk_update_detections(detections);\n\n        PanTiltCommand cmd = tracker.calculatePanTiltCommand();\n        if (cmd.valid) {\n            controlPTZ(cmd.pan_delta, cmd.tilt_delta);\n        }\n\n        usleep(33000);\n    }\n\n    return 0;\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"common-patterns",children:"Common Patterns"}),"\n",(0,i.jsx)(n.h3,{id:"error-handling",children:"Error Handling"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:'// Check if target exists before tracking\nDetectedObject target;\nif (tracker.ai_obj_trk_get_current_target(target)) {\n    // Target is valid, proceed with tracking\n    PanTiltCommand cmd = tracker.calculatePanTiltCommand();\n    // ...\n} else {\n    // No target found or tracking disabled\n    printf("No active target\\n");\n}\n'})}),"\n",(0,i.jsx)(n.h3,{id:"debug-logging",children:"Debug Logging"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:"// Enable debug logging for troubleshooting\nObjectTrackerLib::setLogLevel(LogLevel::LOG_DEBUG);\n\n// Disable logging in production\nObjectTrackerLib::setLogLevel(LogLevel::NONE);\n"})}),"\n",(0,i.jsx)(n.h3,{id:"status-monitoring",children:"Status Monitoring"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:"// Print tracker status\ntracker.printStatus();\n\n// Check MQTT status\nif (mqtt_tracker.isMQTTConnected()) {\n    mqtt_tracker.printMQTTStatus();\n}\n"})}),"\n",(0,i.jsx)(n.h2,{id:"integration-with-gstreamer",children:"Integration with GStreamer"}),"\n",(0,i.jsx)(n.p,{children:"Example showing integration with GStreamer-based detection pipeline:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-cpp",children:"// Callback from GStreamer detection element\nvoid on_detection_callback(std::vector<DetectedObject> detections, void* user_data) {\n    ObjectTrackerLib* tracker = static_cast<ObjectTrackerLib*>(user_data);\n\n    // Update tracker with new detections\n    tracker->ai_obj_trk_update_detections(detections);\n\n    // Calculate pan/tilt command\n    PanTiltCommand cmd = tracker->calculatePanTiltCommand();\n\n    if (cmd.valid) {\n        // Send to PTZ controller\n        send_ptz_command(cmd.pan_delta, cmd.tilt_delta);\n    }\n}\n"})})]})}function m(e={}){const{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}},28453:(e,n,t)=>{t.d(n,{R:()=>c,x:()=>o});var a=t(96540);const i={},r=a.createContext(i);function c(e){const n=a.useContext(r);return a.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:c(e.components),a.createElement(r.Provider,{value:n},e.children)}}}]);